name: Build All APKs

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies & Buildozer
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
        pip install --upgrade pip
        pip install buildozer Cython==0.29.36

    - name: Extract Project Zip
      run: |
        unzip app_project.zip -d extracted_apps
        ls -la extracted_apps

    - name: Find and Build Apps
      run: |
        # الكود راح يبحث عن أي مجلد داخله buildozer.spec ويبنيه تلقائياً
        cd extracted_apps
        # إذا كانت التطبيقات بداخل مجلدات فرعية داخل الـ zip، سننتقل لكل مجلد ونشغل بيلدوزر
        for dir in */; do
          if [ -d "$dir" ]; then
            echo "Building app in $dir"
            cd "$dir"
            if [ -f "buildozer.spec" ]; then
              yes | buildozer -v android debug
            fi
            cd ..
          fi
        done

    - name: Upload APKs Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: my-built-apks
        path: |
          extracted_apps/*/bin/*.apk
          bin/*.apk
